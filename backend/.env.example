PORT=4000
# Example MongoDB connection (replace with your own Atlas URI or local MongoDB)
# MONGO_URI=mongodb+srv://<USER>:<PASSWORD>@cluster0.mongodb.net/peer_tutor?retryWrites=true&w=majority
# For local MongoDB:
# MONGO_URI=mongodb://localhost:27017/peer_tutor_dev
# Use a strong JWT secret in production
# JWT_SECRET=replace_with_a_strong_secret
